# Benchmark 方法

## 在线服务测试

固定数据集、输入/输出长度、请求数、并发或 request rate。warmup 与正式样本分开，并保留失败请求。

## 稳态

请求数量应足以让服务进入稳态。短测试容易被启动、缓存、编译和首次分配影响。

## 样本

建议至少 3 次正式重复。不要只保留最快一次；汇总 median、分位数和波动。

## 单变量原则

做比较时只改变一个明确变量。模型、硬件、dtype、并行、缓存策略或 workload 变化时，应视为新的实验条件。

## 资源

记录设备数量、可见性、显存、CPU、NUMA、通信配置和是否存在其他占用。
